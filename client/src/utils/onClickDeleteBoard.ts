/** @format */

// /** @format */

// import deleteBoard from '@/app/apis/boards/deleteBoard';

// export default async function onClickDeleteBoard(boardId: number) {
// 	const response = await deleteBoard(boardId);

// 	if (response.statusCode === 200) {
// 	}
// }
