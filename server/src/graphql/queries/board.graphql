query GetBoards {
    getBoards {
        author
        title
        boardId
        createdAt
    }
}
query GetBoard($boardId: Int!) {
    getBoard(boardId: $boardId) {
        ...BoardField
    }
}
