'use client';

import { useState } from 'react';
import useBoardList from '../list/hooks';
import _ from 'lodash';

export default function useSearch() {
    const { refetch } = useBoardList();

    return {

    };
}
