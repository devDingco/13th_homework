import FormComponent from './FormComponent'; // Import the client component

const Page = () => {
  return (
    <div>
      <h1>This is the Form Page</h1>
      {/* Render the client component */}
      <FormComponent />
    </div>
  );
};

export default Page;
